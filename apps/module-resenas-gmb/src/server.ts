/**
 * Panel web de aprobación (puerto PANEL_PORT).
 * TODO (Nicolás): Express/Fastify + UI según docs/students/06-guia-estilos.md
 */
const port = Number(process.env.PANEL_PORT ?? 4001);

console.log(`Panel resenas-gmb — implementar servidor en http://localhost:${port}`);
console.log('Ver: docs/students/asignaciones/nicolas-resenas-gmb.md');
process.exit(0);
